# AzureServer01

Terraform configuration for provisioning a Windows Server 2019 virtual machine on Azure with its supporting infrastructure.

## Contents

- `main.tf` defines the resource group, networking, storage, automation account, DNS zone, NIC, and VM
- `variables.tf` defines the configurable input variables
