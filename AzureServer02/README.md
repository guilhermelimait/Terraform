# AzureServer02

Terraform configuration for provisioning an Azure virtual machine together with a small sample website.

## Contents

- `main.tf` defines the Azure infrastructure
- `variables.tf` defines the configurable input variables
- `site/` contains static files intended to be served from the provisioned server
